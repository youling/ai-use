# 00_KERNEL

Kernel 层：稳定权威与接口入口。

Namespace-aware 启动需要入口时先读本层。

包含：

- 机器 L0 规则（`../AGENTS.md`）
- 治理宪法（`../CONSTITUTION.md`）
- 阅读索引（`../READING_MAP.md`）
- 人类可见输出规范（`LANGUAGE_POLICY.md`）

本层不替代 `AGENTS.md`；`AGENTS.md` 仍是机器 L0 唯一规范源。
