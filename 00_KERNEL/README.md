# 00_KERNEL

Kernel 层：稳定规则与核心解释的文档分类。

**本目录不是执行 cold-start 前置，也不定义阅读顺序。** 准备执行/恢复/接管时，第一份 normative rules read 始终是 governance repo 根目录的 `../AGENTS.md`；随后按 `../READING_MAP.md` targeted expansion。

包含：

- 机器 L0 规则（`../AGENTS.md`）
- 治理宪法（`../CONSTITUTION.md`）
- 阅读索引（`../READING_MAP.md`）
- 人类可见输出规范（`LANGUAGE_POLICY.md`）

本层不替代 `AGENTS.md`；`AGENTS.md` 仍是机器 L0 唯一规范源。Namespace 只用于定位文档类别。
