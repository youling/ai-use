# 00_KERNEL

Kernel layer: stable authority and interface entry.

Load first when a namespace-aware bootstrap entry is required.

Contains:

- authority rules
- agent interface pointers

This namespace does not replace `AGENTS.md`.
